\documentclass{ctexart}
\usepackage{amssymb}
\usepackage{enumitem}
\renewcommand{\labelitemi  }{$\bullet$}
\renewcommand{\labelitemii }{$\diamond$}
\renewcommand{\labelitemiii}{$\ast$}
\renewcommand{\labelitemiv }{$\circ$}
\setlist{
  align=left,
  labelwidth=.85em,
  labelsep=0.15em,
  leftmargin=0pt,
}
% Setting global list indentation for each level
\setlist[1]{itemindent=3em}
\setlist[2]{itemindent=3em,leftmargin=2em}
\setlist[3]{itemindent=3em,leftmargin=2em}
\setlist[4]{itemindent=3em,leftmargin=2em}
% Settings for description
\setlist[description]{
  labelindent=2em,
  labelsep*=1em,
  itemindent=1em,
  leftmargin=!,
}

\begin{document}
在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
\begin{itemize} % 设置标签缩进为 2em
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \begin{itemize}
    \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
    \begin{itemize}
      \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
      \begin{itemize}
        \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
      \end{itemize}
    \end{itemize}
  \end{itemize}
\end{itemize}

\begin{enumerate}
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
  \item 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
\end{enumerate}

\begin{description}
  \item[如何自定义列表格式] 在 LaTeX 中，setlist 是 enumitem 宏包提供的命令，用于自定义列表环境（itemize、enumerate、description）的格式和样式。通过 setlist，可以全局或局部设置列表的标签、间距、缩进等属性。
\end{description}

\end{document}